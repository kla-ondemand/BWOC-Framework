[plugin]
name        = "audit-iso-20000-1"
kind        = "audit"
version     = "0.1.0"
description = "ISO/IEC 20000-1 IT Service Management System audit — stub. Runtime deferred to BWOC-EPIC-3."
compat      = ">=2.5.0"
entry       = "audit.sh"

# [config.schema] intentionally omitted. The stub reports every criterion as
# `not_implemented`; there is nothing to configure until the EPIC-3 runtime
# lands. The only workspace-level surface is the universal `enabled` key
# on `[plugins.audit-iso-20000-1]`.
